import { FacilityTypePipe } from './facility-type.pipe';

describe('FacilityTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FacilityTypePipe();
    expect(pipe).toBeTruthy();
  });
});
