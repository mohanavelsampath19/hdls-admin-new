import { BookingstatusPipe } from './bookingstatus.pipe';

describe('BookingstatusPipe', () => {
  it('create an instance', () => {
    const pipe = new BookingstatusPipe();
    expect(pipe).toBeTruthy();
  });
});
