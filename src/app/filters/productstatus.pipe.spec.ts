import { ProductstatusPipe } from './productstatus.pipe';

describe('ProductstatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductstatusPipe();
    expect(pipe).toBeTruthy();
  });
});
